<%= render 'comments/form', comment: @new_comment, post: @new_comment.post  %>
<div id="comments" class="my-2">
  <% @comments.each do |comment| %>
    <%= render 'comments/show', comment: comment %>
  <% end %>
</div>
